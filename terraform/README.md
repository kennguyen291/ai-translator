## MAIN FILE
Terraform configurations including the providers and region.

## LAMBDA FILE
All the configuration for API Gateway endpoint and Lambda function are packages within the custom api_lambda module.
See variables file in the module for detailed explanations.

## GET STARTED
Run command **terraform apply** to create the necessary infrasrtucture, which includes API Gateway endpoint and Lambda function.
